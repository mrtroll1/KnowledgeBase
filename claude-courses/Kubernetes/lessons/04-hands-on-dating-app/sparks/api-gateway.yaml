apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: sparks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels: 
        app: api-gateway
    spec:
      containers:                                                                                                                                                 
      - name: api-gateway
        image: nginx:alpine                                                                                                                                       
        ports:  
        - containerPort: 80
        envFrom:
        - configMapRef:
            name: app-config
        resources:
          requests:
            memory: "32Mi"
            cpu: "50m"
          limits:
            memory: "64Mi"
            cpu: "100m"

---
apiVersion: v1
kind: Service
metadata: 
  name: api-gateway
  namespace: sparks
spec: 
  selector: 
    app: api-gateway
  ports:
  - port: 80
    targetPort: 80