# Lesson 9: Transitions & Teleports — Quiz

## Q1
You have a tab switcher. Without `mode="out-in"`, both tabs briefly appear at the same time during transition. Why does this happen, and how does `mode="out-in"` fix it?

---

## Q2
Write the CSS needed for a `<Transition name="slide">` that slides elements in from the left and out to the right.

---

## Q3
Your `<TransitionGroup>` animates items entering and leaving, but when you reorder the list, items jump to their new positions without animation. What's missing?

---

## Q4
A modal component is deeply nested: `App → Page → Form → ConfirmDialog`. The modal's overlay doesn't cover the full screen because a parent has `overflow: hidden`. How do you fix this without restructuring your components?

---

## Q5
When would you use JavaScript transition hooks (`@enter`, `@leave`) instead of pure CSS transitions?
